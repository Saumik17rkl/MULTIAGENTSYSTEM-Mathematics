# Required: At least one of these must be set
GOOGLE_API_KEY=your_gemini_api_key_here
GROQ_API_KEY=your_groq_api_key_here

# Optional: Set default LLM provider (gemini or groq)
# If not set, will try Gemini first, then fall back to Groq
# DEFAULT_LLM_PROVIDER=groq

# Optional: Set default model for each provider
# GEMINI_MODEL=gemini-pro
# GROQ_MODEL=llama-3.3-70b-versatile

# Optional: Debug mode (true/false)
# DEBUG=false

# Optional: Log level (debug, info, warning, error, critical)
# LOG_LEVEL=info
